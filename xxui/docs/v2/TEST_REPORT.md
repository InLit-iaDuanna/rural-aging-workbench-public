# V2.0 工程验证报告

日期：2026-09-10。以下是开发机实测，不是工作人员或真实村庄验收。

| 检查 | 实测结果 |
|---|---|
| TypeScript 类型检查 | 通过 |
| Vinext 生产构建 | 通过；保留体积提示 |
| 自动测试 | 5 个文件、18 项全部通过 |
| PostgreSQL 真实业务链 | 通过：登录、反馈、问题、规则推演、草案、独立复核、幂等确认、行动及回访 |
| 同步重试与跨项目访问 | 同步不重复创建；跨项目访问拒绝；版本冲突返回 409 |
| LangGraph PostgreSQL 检查点 | 不同图实例持久化暂停与恢复通过 |
| 独立数据库恢复演练 | 通过，恢复后逐表行数与备份一致；见本机 work/restore-report.json |
| CLI 单次小结构调用 | CodeBuddy 与 Codex 均曾成功；完整复杂调用仍有超时，见下文 |
| 腾讯地址服务 | 真实请求 status=0；服务端登录接口调用通过；匿名调用返回 401 |
| 凭据检查 | 源码、文档、示例及前端构建中未出现用户 Key/SK |
| npm 依赖审计 | 修补已发现漏洞后为 0 项 |
| 浏览器及现场验收 | 未进行自动浏览器操作或真实工作人员验收 |

自动测试覆盖未知尺度、路段属性未知、确定性推演及非法决策、容量、项目权限、冲突、重复确认、CLI 取消和超时、工作流暂停恢复。全部单元测试通过不等于所有生产环境组合都通过。

## 性能和 AI 失败记录

早期小结构连通性实测：CodeBuddy 约 7.4 秒，Codex 约 14.3 秒。复杂工作流测试发生结构不合规后修复超时（总计约 168.5 秒），以及单次 120 秒超时；均未伪造成功结果。运行日志保存执行器版本、每次耗时、可用 Token 用量和成功/失败状态。当前样本不足以报告稳定成功率或试点推演误差。

AI 输出校验、参数和提示配置仍须在目标执行主机复验。人工等待不占用 CLI 调用超时。超时不自动更换执行器，不自动写入“通过”。

## 证据范围

本机 `work/` 保留合成验收报告及私有运行数据，不进入公开源码或构建。恢复测试使用独立新数据库，不覆盖正在运行的数据。真实授权、现场采集、工程效果及村方决定尚无验收记录。

## 本轮追加验证

- 6 个测试文件、19 项通过；新增村庄保存、重读、位置冲突、角色权限和不凭空生成路网。
- CodeBuddy 使用 glm-5.3-flash、minimal effort，仍保留每次 120 秒上限；知识咨询与 Codex 复核 23.393 秒完成，复核返回 conditional 并指出来源误用。
- AI 整链 26.178 秒进入 awaiting_approval，包含真实路线工具、CodeBuddy 分析及草案、Codex 独立复核。
- 本机 skill 校验通过，82 个片段保留来源及未核验身份。
- 一次真实 HTTP 联测因模型把段落编号写成条目 ID 而被拒绝；已将输出引用收紧为本次检索条目的枚举，继续验证。
- 修正后的真实 HTTP 整链通过：经 localhost:3000 同源登录，腾讯地图解析宏村、保存并重新读取真实坐标档案，检索知识后提交后台咨询，22.417 秒完成，Codex verdict=pass，4 条引用均来自本次检索。测试档案明确标注“接口验收，不代表村庄试点”。报告保存在 work/village-live-report.json。
